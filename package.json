{
  "name": "finesse-waitlist",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vercel dev",
    "check": "node --check assets/site.js && node --check api/waitlist.js"
  }
}
